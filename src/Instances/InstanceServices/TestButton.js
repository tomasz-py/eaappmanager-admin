// import React from "react";

// import { EditButton } from "react-admin";

// test = () => {
//   var a = Server.find(
//     { Serverdetails: { owner: [{ posts: "images" }, "orders"] } },
//     function(err, server) {
//       server.forEach(function(server) {
//         // post.owner points to the relation method instead of the owner instance
//         var p = server.toJSON();
//         console.log(p);
//       });
//       //...
//     }
//   );
//   return 1;
// };

// const Buttontest = ({ record }) => {
//   return <EditButton onClick={test} />;
// };

// export default Buttontest;
